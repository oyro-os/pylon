/target

# JetBrains IDE files
/.idea

# Local parity references — cloned upstream libs used to reconcile wire/auth
# behavior (pusher-js, pusher-http-node, soketi). See reference/README.md.
/reference

# Local app config holds real credentials — never commit it.
# Copy apps.example.json to apps.json and fill in your values.
/apps.json

# Internal research/design docs — kept locally, purged from public history.
/docs/superpowers

# Python venv for docs builds (local only)
/.venv

# MkDocs build output
/website/site
